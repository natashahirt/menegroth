---
id: chec-b0000002
type: checkpoint
project: menegroth
parent_id: plan-b0000001
title: 'Setup status 2026-07-12: angelo installed & configured'
node_label: 'Setup status 2026-07-12'
tags: setup,install,status
status: active
open_threads: 0
success: 'null'
files: ''
session_id: ''
created_at: '2026-07-12T20:30:00+00:00'
updated_at: '2026-07-12T20:30:00+00:00'
---
# Setup status - 2026-07-12

**Done**
- Installed `angelo` 1.7.8 with the `[zettelkasten]` extra into the conda base env (`/opt/miniconda3`), via a full git clone + `pip install` with `--resume-retries` (the network to GitHub/PyPI is heavily throttled; partial-clone and single-shot downloads kept timing out).
- Ran `angelo init --cursor --with-zettelkasten`: scaffolded `.cursor/mcp.json` (memory, coordinator, zettelkasten, memory-artifacts servers), rules (memory, coordinator, zettelkasten, grounded-extraction, stream, operations), and the onboard-project/close-branch skills.
- Ran `angelo doctor --fix`: rewrote `mcp.json` commands to absolute console-script paths under `/opt/miniconda3/bin` so Cursor's GUI-launch PATH can find them.
- Initialized this `.memory/` research tree.

**Blocked / degraded**
- Embeddings are OFF for now (`MEMORY_SKIP_EMBEDDINGS`): the model2vec model (`minishlab/potion-retrieval-32M`, ~120MB) is fetched from a GitHub-release mirror (HF fallback), and both GitHub and the HF large-file CDN return 0 bytes on this network. Memory search degrades to keyword matching until the model is cached; it will backfill on a faster network or when the in-editor server can fetch it.
- The in-editor angelo MCP servers are not yet live in this Cursor session (Cursor loads new `mcp.json` servers only on reload).
