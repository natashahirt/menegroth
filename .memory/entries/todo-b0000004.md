---
id: todo-b0000004
type: todo
project: menegroth
parent_id: plan-b0000001
title: Resolve kglite native crash so in-editor MCP servers run
node_label: Resolve kglite native crash
tags: kglite,todo,mcp,blocker,resolved
status: resolved
open_threads: 0
success: 'true'
files: ''
session_id: ''
created_at: '2026-07-12T20:30:00+00:00'
updated_at: '2026-07-12T21:55:00+00:00'
---
**RESOLVED (2026-07-12) via option (1): a dedicated Python 3.11 venv.** See checkpoint `chec-2f60a91a`. Created `~/.venvs/angelo` on Python 3.11.1, installed `angelo[zettelkasten]` 1.7.8, and repointed all four servers in `.cursor/mcp.json` to `~/.venvs/angelo/bin`. `import memory.server` and `KnowledgeGraph()` no longer SIGFPE (the crash was specific to the conda Python 3.13 native stack), and MCP `tools/list` now returns tools (memory 16, coordinator 15, zettelkasten 21).

---

_Original issue:_ The `angelo-memory` / `angelo-coordinator` / `angelo-zettelkasten` console scripts used `/opt/miniconda3/bin/python3.13`, which segfaults when `memory.server` builds the kglite graph. Until fixed, the in-editor servers, the coordinator workflows, and `create_extraction_graph` could not run. Options considered: (1) a dedicated Python 3.11 venv for angelo and repoint `mcp.json`; (2) reinstall/rebuild `kglite`; (3) isolate the offending import and set thread/FP env (RAYON_NUM_THREADS, etc.). Reproduce with: `MEMORY_SKIP_EMBEDDINGS=1 python -c 'import memory.server'`.
